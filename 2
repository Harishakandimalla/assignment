#include <stdio.h>

int main()
{
    int n, i;
    printf("Input the number of elements to store in the array : ");
    scanf("%d", &n);
    int arr[n];
    printf("Input %d number of elements in the array :\n", n);
    
    // loop to read values into array
    for(i=0; i<n; i++)
    {
        printf("element - %d : ", i);
        scanf("%d", &arr[i]);
    }
    
    // displaying the values in array
    printf("\nThe values stored into the array are : \n");
    for(i=0; i<n; i++)
    {
        printf("%d ", arr[i]);
    }
    
    // displaying the values in array in reverse order
    printf("\nThe values stored into the array in reverse are : \n");
    for(i=n-1; i>=0; i--)
    {
        printf("%d ", arr[i]);
    }
    printf("\n");
    return 0;
}
output:
Input the number of elements to store in the array : 4
Input 4 number of elements in the array :
element - 0 : 2
element - 1 : 5
element - 2 : 7
element - 3 : 9

The values stored into the array are :
2 5 7 9
The values stored into the array in reverse are :
9 7 5 2
